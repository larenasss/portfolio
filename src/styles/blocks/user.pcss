.user {
   display: flex;
   flex-direction: column;

   @include phones {
      margin-bottom: 50px;
   }
}

.user__avatar {
   display: block;
   width: 200px;
   height: 200px;
   overflow: hidden;
   border-radius: 50%;
   margin-bottom: 75px;
   align-self: center;
   z-index: 2;

   @include phones {
      margin-bottom: 30px;
   }
}

.user__pic {
   display: block;
   max-width: 100%;
   max-height: 100%;
}

.user__deck-item {
   display: flex;
   justify-content: space-between;
   margin-bottom: 20px;

   &:last-child {
      margin-bottom: 0;
   }
}

.user__key {
   opacity: 0.15;
   margin-right: 10px;
}

.user__value {
   font-weight: 600;
   white-space: nowrap;
}